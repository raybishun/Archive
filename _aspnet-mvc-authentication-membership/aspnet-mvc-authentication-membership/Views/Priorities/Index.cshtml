@model IEnumerable<aspnet_mvc_authentication_membership.Models.Priority>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PriorityDesc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PriorityDesc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PriorityId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PriorityId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PriorityId })
        </td>
    </tr>
}

</table>
