using System;
using System.Text;

namespace _01_ImplicitTyping
{
    class Program
    {
        // var CS0825 = "may only appear with a local variable declaration or in script code";

        static void Main(string[] args)
        {
            // Implicitly-typed variables must be initialized
            // because the compiler needs to figure out the data type
            
            // var firstName; 

            var sb = new StringBuilder();


            Console.ReadLine();
        }
    }
}
